export enum ApplicationStatus {
  PENDING = 'pending',     // vừa apply
  REVIEWED = 'reviewed',   // employer đã xem
  SHORTLISTED = 'shortlisted',
  REJECTED = 'rejected',
  ACCEPTED = 'accepted',
  WITHDRAWN = 'withdrawn',
}